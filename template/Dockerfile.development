FROM node:26.5.1-bookworm-slim

RUN apt-get update \
  && apt-get install --yes --no-install-recommends procps \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /app

ENV NODE_ENV=development

EXPOSE 8000 8002

CMD ["sh", "-c", "npm ci && exec node --import=tsx develop.ts"]
